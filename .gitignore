# исходники/черновики — не нужны для сайта, слишком тяжёлые для гита
порфтолио/
превью/
перс, эмоции/
# исходники видео для лайтбокса "С кем работал" — чистые копии уже лежат в assets/video/clients/
видео для С кем работал/

# бэкапы, которые делает /__api/save при каждом сохранении из админки
backups/

.claude/

# токен Netlify для деплоя — никогда не коммитить
.netlify-token
.netlify/

# собирается автоматически перед деплоем на Netlify (index.html + assets + easter-egg видео)
_deploy/

# токен GitHub для деплоя — никогда не коммитить
.github-token

